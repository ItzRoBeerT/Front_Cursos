import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Front_Cursos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
